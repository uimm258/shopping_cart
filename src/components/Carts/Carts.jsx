import React from "react";
// import './Carts.css';

const Carts = ({ state, dispatch }) => {
    const { products, cart } = state

    return (
        <div className="carts">
            <h2>Carts</h2>
        </div>
    )
}

export default Carts