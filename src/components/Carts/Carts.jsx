import React from "react";

const Carts = ({ state, dispatch }) => {
    const { products, cart } = state

    return (
        <>
            <p>Cart is pending....</p>
        </>
    )
}

export default Carts